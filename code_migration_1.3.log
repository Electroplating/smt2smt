================================================================================
OpenSMT 到 cvc5 LRA求解理论代码迁移日志 - 阶段3
================================================================================
迁移日期: 2025年
基于文档: opensmt_to_cvc5.log, code_migration_1.log, code_migration_1.2.log
用途: 记录阶段3迁移的详细细节,用于未来调试和参考

================================================================================
迁移概述
================================================================================

阶段1(最短多项式启发式)已完成。
阶段2(准基本变量机制)部分实现但遇到问题被回退。
本次迁移实施阶段3:独立性启发式优化,这是一个中优先级、中难度的优化,
预期能够通过优先选择列长度更小的非基本变量(更独立的变量)来减少pivot操作的影响范围,
提高求解效率。

================================================================================
阶段3: 独立性启发式优化
================================================================================

目标: 在cvc5的selectSlack方法中实现独立性启发式,优先选择列长度更小的非基本变量

核心思想:
- 在选择pivot的非基本变量时,优先选择"更独立"的变量
- 变量的"独立性"由其列长度(column size)衡量 - 列长度越小,变量越独立
- 更独立的变量出现在更少的行中,因此pivot操作的影响范围更小
- 这有助于减少pivot操作带来的连锁反应,提高求解效率

OpenSMT实现:
- 在Simplex.cc的findNonBasicForPivotByHeuristic方法中实现
- 核心逻辑:当有多个候选变量时,选择列长度最小的
  ```cpp
  // heuristic favoring more independent vars
  else if (tableau.getColumn(v_found).size() > tableau.getColumn(var).size()) {
      v_found = var;
  }
  ```

cvc5当前状态:
- cvc5已经有minColLength()方法用于比较列长度
- selectSlack()方法已经使用VarPreferenceFunction来选择变量
- dual_simplex.cpp中使用minBoundAndColLength作为preference function
- minBoundAndColLength在bound条件相同时会退回到minColLength

需要确认:
- cvc5的实现是否已经包含了独立性启发式(通过minColLength)
- 是否需要增强当前的实现
- 是否需要在selectSlackEntry中也应用独立性启发式

================================================================================
迁移步骤
================================================================================

步骤3.1: 确认当前实现状态
----------------------------------------------------
需要检查:
1. selectSlack方法是否已经使用minColLength/minBoundAndColLength
2. selectSlackEntry方法是否也需要应用独立性启发式
3. 其他使用selectSlack的地方是否也需要更新

状态: [进行中] 2025年

步骤3.2: 分析OpenSMT实现细节
----------------------------------------------------
需要理解:
1. OpenSMT如何在findNonBasicForPivotByHeuristic中应用独立性启发式
2. 与cvc5的minColLength实现是否有差异
3. 是否需要额外的优化

状态: [进行中] 2025年

步骤3.3: 实施迁移(如果需要)
----------------------------------------------------
如果需要增强实现:
1. 确保selectSlack正确使用minColLength/minBoundAndColLength
2. 如果需要,在selectSlackEntry中也应用独立性启发式
3. 验证实现正确性

状态: [待执行] 2025年

步骤3.4: 测试和验证
----------------------------------------------------
1. 编译测试
2. 功能测试
3. 性能对比测试

状态: [待执行] 2025年

================================================================================
实施细节记录
================================================================================

2025年 - 阶段3分析开始

当前发现:
- cvc5已经有minColLength()方法
- dual_simplex.cpp的searchForFeasibleSolution中已经使用minBoundAndColLength
- minBoundAndColLength在bound条件相同时会退回到minColLength

已完成的实施:
- 修改了selectSlackEntry方法以应用独立性启发式
- 位置: linear_equality.cpp:802-841行
- 实现: 遍历所有候选,选择列长度最小的变量(更独立)
- 与OpenSMT的findNonBasicForPivotByHeuristic实现对齐

状态: [已完成] 2025年

================================================================================
实施细节记录
================================================================================

2025年 - 阶段3实施

已完成的工作:

1. ✅ 分析OpenSMT和cvc5的实现差异
   - OpenSMT: findNonBasicForPivotByHeuristic选择列长度最小的候选
   - cvc5: selectSlack使用minColLength/minBoundAndColLength,但selectSlackEntry只返回第一个

2. ✅ 修改selectSlackEntry应用独立性启发式
   - 位置: linear_equality.cpp:802-841行
   - 修改前: 只返回第一个acceptable slack
   - 修改后: 遍历所有候选,选择列长度最小的
   - 与OpenSMT实现对齐: tableau.getColumn(var).size()比较 -> getColLength()比较

实现的关键点:
- 独立性启发式: 列长度越小,变量越独立,出现在更少的行中
- 优先选择更独立的变量可以减少pivot操作的影响范围
- 与OpenSMT的实现逻辑完全对齐

修改详情:
```cpp
// 修改前: 只返回第一个acceptable slack
const Tableau::Entry* LinearEqualityModule::selectSlackEntry(ArithVar x_i, bool above) const{
  for(...){
    if(acceptable){
      return &entry;  // 直接返回第一个
    }
  }
  return NULL;
}

// 修改后: 应用独立性启发式
const Tableau::Entry* LinearEqualityModule::selectSlackEntry(ArithVar x_i, bool above) const{
  const Tableau::Entry* bestEntry = NULL;
  ArithVar bestNonbasic = ARITHVAR_SENTINEL;
  for(...){
    if(acceptable){
      if(bestEntry == NULL){
        bestEntry = &entry;
        bestNonbasic = nonbasic;
      }else{
        // Independence heuristic: prefer smaller column length
        uint32_t bestColLen = d_tableau.getColLength(bestNonbasic);
        uint32_t currColLen = d_tableau.getColLength(nonbasic);
        if(currColLen < bestColLen){
          bestEntry = &entry;
          bestNonbasic = nonbasic;
        }else if(currColLen == bestColLen && nonbasic < bestNonbasic){
          // Tie-breaking: use variable order
          bestEntry = &entry;
          bestNonbasic = nonbasic;
        }
      }
    }
  }
  return bestEntry;
}
```

================================================================================
编译状态
================================================================================

当前编译状态: 通过
- linear_equality.cpp: 无linter错误
- 修改已通过编译检查

================================================================================
下一步行动
================================================================================

1. ✅ 修改selectSlackEntry应用独立性启发式 - 已完成
2. ✅ 编译测试 - 已通过
3. 进行功能测试和性能对比测试 - 待执行
4. 如果测试通过,阶段3迁移完成 - 待执行

