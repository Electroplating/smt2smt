================================================================================
CVC5 LRA (Linear Real Arithmetic) 求解器算法分析
================================================================================
生成时间: 2025
基于: cvc5 源代码 (src/theory/arith/linear/)

================================================================================
一、算法概述
================================================================================

CVC5 的 LRA 求解器基于 "Simplex for DPLL(T)" 算法，参考论文：
http://research.microsoft.com/en-us/um/people/leonardo/cav06.pdf

核心思想：
1. 使用对偶单纯形法 (Dual Simplex) 求解线性实数算术约束
2. 维护一个表 (Tableau) 和部分模型 (Partial Model)
3. 通过基变量 (basic variables) 和非基变量 (non-basic variables) 的转换来寻找可行解
4. 使用 DeltaRational 来处理严格不等式（通过无穷小量 delta）

主要类：
- TheoryArithPrivate: QF_LRA 的主要实现类
- LinearSolver: 线性求解器的包装类
- DualSimplexDecisionProcedure: 对偶单纯形法决策过程
- ConstraintDatabase: 约束数据库
- Tableau: 单纯形表
- ArithVariables: 算术变量和部分模型
- ErrorSet: 错误变量集合（违反约束的变量）

================================================================================
二、核心数据结构
================================================================================

2.1 DeltaRational (theory/arith/delta_rational.h)
--------------------------------------------------
用途：表示形如 c + k*delta 的数，其中 delta 是系统级的符号无穷小量
作用：处理严格不等式（<, >）

关键字段：
- Rational c: 非无穷小部分
- Rational k: 无穷小系数

关键方法：
- sgn(): 返回符号
- infinitesimalSgn(): 返回无穷小部分的符号
- cmp(): 比较两个 DeltaRational
- operator+, -, *, /: 算术运算

2.2 ArithVar (theory/arith/linear/arithvar.h)
---------------------------------------------
类型：typedef uint32_t ArithVar
用途：算术变量的内部表示（索引）

2.3 Constraint (theory/arith/linear/constraint.h)
-------------------------------------------------
用途：表示一个约束（上界、下界、等式、不等式）

关键字段：
- ArithVar d_variable: 关联的变量
- ConstraintType d_type: 约束类型 (LowerBound, UpperBound, Equality, Disequality)
- DeltaRational d_value: 约束值
- ConstraintP d_negation: 否定约束的指针
- Node d_literal: 对应的 SMT 节点
- bool d_canBePropagated: 是否可以传播
- AssertionOrder d_assertionOrder: 断言顺序
- ConstraintRuleID d_crid: 约束规则 ID（用于证明）

约束类型：
- LowerBound: x >= c 或 x > c
- UpperBound: x <= c 或 x < c
- Equality: x = c
- Disequality: x != c

2.4 ConstraintDatabase (theory/arith/linear/constraint.h)
---------------------------------------------------------
用途：管理所有约束的数据库

关键数据结构：
- std::vector<PerVariableDatabase*> d_varDatabases: 每个变量的约束数据库
- NodetoConstraintMap d_nodetoConstraintMap: 从节点到约束的映射
- context::CDQueue<ConstraintCP> d_toPropagate: 待传播的约束队列
- CDConstraintList d_antecedents: 证明的前件列表

PerVariableDatabase:
- ArithVar d_var: 变量
- SortedConstraintMap d_constraints: 按值排序的约束映射

ValueCollection:
- 绑定相同 DeltaRational 值的凸约束集合
- ConstraintP d_lowerBound, d_upperBound, d_equality, d_disequality

2.5 ArithVariables (theory/arith/linear/partial_model.h)
----------------------------------------------------------
用途：管理变量的赋值和上下界

关键字段：
- VarInfoVec d_vars: 变量信息向量
- DenseMap<DeltaRational> d_safeAssignment: 安全赋值映射
- ArithVar d_numberOfVariables: 变量数量
- NodeToArithVarMap d_nodeToArithVarMap: 节点到变量的映射

VarInfo:
- ArithVar d_var: 变量索引
- DeltaRational d_assignment: 当前赋值
- ConstraintP d_lb, d_ub: 下界和上界约束
- int d_cmpAssignmentLB, d_cmpAssignmentUB: 赋值与边界的比较结果
- ArithType d_type: 类型 (Real/Integer)
- Node d_node: 对应的节点
- bool d_auxiliary: 是否为辅助变量

关键方法：
- setAssignment(): 设置变量赋值
- setLowerBoundConstraint(): 设置下界约束
- setUpperBoundConstraint(): 设置上界约束
- getAssignment(): 获取赋值
- getUpperBound(), getLowerBound(): 获取上下界
- assignmentIsConsistent(): 检查赋值是否一致

2.6 Tableau (theory/arith/linear/tableau.h)
--------------------------------------------
用途：维护单纯形表，表示线性等式系统

继承自：Matrix<Rational>

关键数据结构：
- BasicToRowMap d_basic2RowIndex: 基变量到行索引的映射
- RowIndexToBasicMap d_rowIndex2basic: 行索引到基变量的映射

关键方法：
- isBasic(ArithVar v): 检查变量是否为基变量
- basicToRowIndex(ArithVar x): 获取基变量的行索引
- rowIndexToBasic(RowIndex rid): 获取行对应的基变量
- addRow(): 添加行
- pivot(): 执行转轴操作
- removeBasicRow(): 移除基变量行

表结构：
- 每行表示一个基变量：basicVar = sum(coeffs[i] * variables[i])
- 基变量系数为 -1

2.7 LinearEqualityModule (theory/arith/linear/linear_equality.h)
-----------------------------------------------------------------
用途：维护 Tableau 和 PartialModel 之间的关系

关键字段：
- ArithVariables& d_variables: 变量引用
- Tableau& d_tableau: 表引用
- BasicVarModelUpdateCallBack d_basicVariableUpdates: 基变量更新回调
- BorderHeap d_increasing, d_decreasing: 边界堆
- BoundInfoMap& d_btracking: 边界信息映射

关键方法：
- update(ArithVar x_i, const DeltaRational& v): 更新非基变量赋值
- pivotAndUpdate(ArithVar x_i, ArithVar x_j, const DeltaRational& v): 转轴并更新
- selectSlack(): 选择松弛变量
- propagateRow(): 传播行约束
- generateConflictAboveUpperBound(): 生成上界冲突
- generateConflictBelowLowerBound(): 生成下界冲突

2.8 ErrorSet (theory/arith/linear/error_set.h)
-----------------------------------------------
用途：管理违反约束的变量集合

关键数据结构：
- ErrorInfoMap d_errInfo: 错误信息映射
- FocusSet d_focus: 焦点堆（优先级队列）
- ArithVarVec d_outOfFocus: 焦点外的变量
- ArithVarVec d_signals: 信号列表

ErrorInformation:
- ArithVar d_variable: 变量
- ConstraintP d_violated: 违反的约束
- int d_sgn: 符号（>0 表示违反下界，<0 表示违反上界）
- bool d_relaxed: 是否已松弛
- bool d_inFocus: 是否在焦点中
- std::unique_ptr<DeltaRational> d_amount: 违反量

关键方法：
- signalVariable(ArithVar var): 标记变量需要检查
- popSignal(): 从信号列表弹出变量并加入错误集
- topFocusVariable(): 获取焦点堆顶变量
- computeDiff(ArithVar x): 计算变量与边界的差值

2.9 DualSimplexDecisionProcedure (theory/arith/linear/dual_simplex.h)
----------------------------------------------------------------------
用途：对偶单纯形法决策过程

关键字段：
- DenseMultiset d_pivotsInRound: 本轮转轴次数统计

关键方法：
- findModel(bool exactResult): 寻找模型
- dualFindModel(bool exactResult): 对偶单纯形法主过程
- searchForFeasibleSolution(uint32_t maxIterations): 搜索可行解

================================================================================
三、关键函数
================================================================================

3.1 主要求解流程函数
---------------------

TheoryArithPrivate::preCheck(Theory::Effort level, bool newFacts)
位置: theory_arith_private.cpp:3083
功能：检查前处理
- 清空传播列表
- 如果有新事实，设置状态为 UNKNOWN
- 记录之前的状态

TheoryArithPrivate::preNotifyFact(TNode fact)
位置: theory_arith_private.cpp:3102
功能：处理新事实
- 将事实转换为约束
- 调用 assertionCases() 处理约束

TheoryArithPrivate::postCheck(Theory::Effort effortLevel)
位置: theory_arith_private.cpp:3112
功能：检查后处理
- 处理学习的边界
- 如果有冲突，输出冲突
- 调用 solveRealRelaxation() 求解实数松弛
- 如果需要，调用 solveInteger() 求解整数

TheoryArithPrivate::solveRealRelaxation(Theory::Effort effortLevel)
位置: theory_arith_private.cpp:2944
功能：求解实数松弛
- 处理边界队列
- 选择单纯形法（DualSimplex, FCSimplex, SOISimplex, AttemptSolutionSDP）
- 调用 simplex.findModel() 寻找模型
- 返回状态 (SAT/UNSAT/UNKNOWN)

3.2 约束处理函数
-----------------

TheoryArithPrivate::constraintFromFactQueue(TNode assertion)
位置: theory_arith_private.cpp
功能：从事实队列创建约束
- 解析断言节点
- 在约束数据库中查找或创建约束
- 返回约束指针

TheoryArithPrivate::assertionCases(ConstraintP c)
位置: theory_arith_private.cpp
功能：处理约束断言
- 根据约束类型调用相应的处理函数：
  - AssertLower(): 处理下界
  - AssertUpper(): 处理上界
  - AssertEquality(): 处理等式
  - AssertDisequality(): 处理不等式

TheoryArithPrivate::AssertLower(ConstraintP constraint)
位置: theory_arith_private.cpp
功能：断言下界约束
- 检查是否与当前下界冲突
- 如果更强，更新下界
- 更新变量赋值
- 如果冲突，返回 true

TheoryArithPrivate::AssertUpper(ConstraintP constraint)
位置: theory_arith_private.cpp
功能：断言上界约束
- 检查是否与当前上界冲突
- 如果更强，更新上界
- 更新变量赋值
- 如果冲突，返回 true

3.3 单纯形法函数
-----------------

DualSimplexDecisionProcedure::dualFindModel(bool exactResult)
位置: dual_simplex.cpp
功能：对偶单纯形法主过程
- 初始化错误集
- 调用 searchForFeasibleSolution() 搜索可行解
- 处理冲突或返回 SAT

DualSimplexDecisionProcedure::searchForFeasibleSolution(uint32_t remainingIterations)
位置: dual_simplex.cpp:144
功能：搜索可行解
算法流程：
1. 当错误集非空且还有迭代次数时：
   a. 从焦点堆中选择违反约束的基变量 x_i
   b. 确定转轴方向（上界或下界）
   c. 选择非基变量 x_j 作为转轴变量
      - 使用启发式规则（minBoundAndColLength）
      - 如果转轴次数过多，使用变量序规则（minVarOrder）
   d. 执行转轴操作：pivotAndUpdate(x_i, x_j, newValue)
   e. 更新错误集
   f. 检查冲突
2. 如果错误集为空，返回 SAT
3. 如果检测到冲突，返回 UNSAT

LinearEqualityModule::pivotAndUpdate(ArithVar x_i, ArithVar x_j, const DeltaRational& v)
位置: linear_equality.cpp
功能：转轴并更新
- x_i: 基变量（离开基）
- x_j: 非基变量（进入基）
- v: x_j 的新值
步骤：
1. 更新 x_j 的赋值为 v
2. 更新 x_i 所在行的所有基变量赋值
3. 执行表转轴：交换 x_i 和 x_j 的角色
4. 更新错误集

LinearEqualityModule::selectSlack(ArithVar x_i, VarPreferenceFunction pf)
位置: linear_equality.h:523
功能：选择松弛变量
- x_i: 违反约束的基变量
- pf: 变量偏好函数
- 返回：可以"吸收松弛"的非基变量
条件：
- 如果 x_i 违反下界且系数 a_ij < 0，需要 x_j < 上界
- 如果 x_i 违反下界且系数 a_ij > 0，需要 x_j > 下界
- 如果 x_i 违反上界且系数 a_ij > 0，需要 x_j < 上界
- 如果 x_i 违反上界且系数 a_ij < 0，需要 x_j > 下界

3.4 冲突生成函数
-----------------

LinearEqualityModule::generateConflictAboveUpperBound(NodeManager* nm, ArithVar conflictVar, FarkasConflictBuilder& rc)
位置: linear_equality.h:637
功能：生成上界冲突
- 使用 Farkas 引理构建冲突
- 返回冲突约束

LinearEqualityModule::minimallyWeakConflict(NodeManager* nm, bool aboveUpper, ArithVar basicVar, FarkasConflictBuilder& rc)
位置: linear_equality.cpp
功能：构建最弱冲突
- 从行中收集约束
- 使用最弱的约束构建 Farkas 证明
- 返回冲突约束

3.5 传播函数
------------

TheoryArithPrivate::propagateCandidatesNew()
位置: theory_arith_private.cpp
功能：传播候选变量
- 处理更新的边界
- 对每个候选行尝试传播

TheoryArithPrivate::propagateCandidateRow(RowIndex rid)
位置: theory_arith_private.cpp
功能：传播候选行
- 检查行是否可以传播
- 尝试单例传播或完全传播

LinearEqualityModule::propagateRow(ConstraintCPVec& into, RowIndex ridx, bool rowUp, ConstraintP c, RationalVectorP farkas)
位置: linear_equality.cpp
功能：传播行约束
- 从行中导出约束
- 构建 Farkas 系数（如果启用证明）

================================================================================
四、求解流程详解
================================================================================

4.1 初始化阶段
--------------
1. TheoryArithPrivate 构造函数初始化所有组件
2. finishInit() 完成初始化（如果启用一致性管理器）

4.2 预处理阶段
--------------
1. preRegisterTerm(TNode n): 预注册项
   - 解析项为多项式
   - 创建变量
   - 设置初始值

2. setupAtom(TNode atom): 设置原子
   - 解析比较操作
   - 在约束数据库中创建约束

4.3 检查阶段（check）
---------------------

流程：
1. preCheck(): 预处理检查
   - 清空传播列表
   - 如果有新事实，重置状态

2. preNotifyFact(): 处理每个新事实
   - 转换为约束
   - 调用 assertionCases() 处理

3. postCheck(): 后处理检查
   a. 处理学习的边界
   b. 如果有冲突，输出并返回
   c. 调用 solveRealRelaxation() 求解实数松弛
   d. 如果需要，调用 solveInteger() 处理整数

4.4 实数松弛求解（solveRealRelaxation）
----------------------------------------

流程：
1. 处理边界队列
2. 选择单纯形法：
   - DualSimplexDecisionProcedure（默认）
   - FCSimplexDecisionProcedure
   - SumOfInfeasibilitiesSPD
   - AttemptSolutionSDP
3. 调用 simplex.findModel(exactResult)
4. 返回状态

4.5 对偶单纯形法（dualFindModel）
----------------------------------

流程：
1. 初始化错误集：
   - 将所有违反约束的基变量加入错误集
   - 构建焦点堆

2. searchForFeasibleSolution() 主循环：
   while (错误集非空 && 有迭代次数):
     a. 从焦点堆选择违反最严重的基变量 x_i
     b. 确定方向（违反上界还是下界）
     c. 选择非基变量 x_j：
        - 启发式：minBoundAndColLength（最小边界和列长度）
        - 如果转轴次数过多：minVarOrder（最小变量序）
     d. 计算 x_j 的新值，使 x_i 满足约束
     e. 执行 pivotAndUpdate(x_i, x_j, newValue)
     f. 更新错误集
     g. 检查冲突

3. 如果错误集为空：返回 SAT
4. 如果检测到冲突：返回 UNSAT

4.6 转轴操作（pivotAndUpdate）
-------------------------------

步骤：
1. 更新非基变量 x_j 的赋值
2. 更新 x_i 所在行的所有基变量：
   newValue(basic) = oldValue(basic) + coeff * (newValue(x_j) - oldValue(x_j))
3. 执行表转轴：
   - 交换 x_i 和 x_j 的角色
   - 更新表的行和列
4. 通知错误集变量值已更新

4.7 冲突检测和生成
-------------------

当基变量违反约束且无法通过转轴修复时：
1. 检测冲突：
   - 检查是否所有非基变量都在边界上
   - 检查行的上下界是否矛盾

2. 生成冲突：
   - 使用 Farkas 引理
   - 收集行中的约束
   - 构建最弱冲突（使用最弱的约束）

3. 输出冲突：
   - 构建冲突节点
   - 发送到输出通道

================================================================================
五、关键算法细节
================================================================================

5.1 DeltaRational 的使用
-------------------------
- 严格不等式 x < c 表示为 x <= c - delta
- 严格不等式 x > c 表示为 x >= c + delta
- delta 是系统级的符号无穷小量
- 比较时先比较非无穷小部分，再比较无穷小部分

5.2 变量选择策略
-----------------
1. 启发式规则（minBoundAndColLength）：
   - 优先选择没有边界的变量
   - 其次选择列长度最小的变量

2. Bland 规则（minVarOrder）：
   - 选择变量序最小的变量
   - 用于避免循环

3. 转轴阈值：
   - 如果变量转轴次数 >= arithPivotThreshold
   - 切换到变量序规则

5.3 错误集管理
--------------
1. 三种模式：
   - Collection: 被动收集，不维护堆结构
   - Difference Queue: 使用变量与边界的差值排序
   - Variable Order Queue: 使用变量序排序

2. 焦点堆（FocusSet）：
   - 使用优先级队列
   - 根据选择规则排序
   - 只包含需要处理的变量

5.4 边界传播
------------
1. 单例传播（attemptSingleton）：
   - 行中只有一个可能的变量
   - 直接传播

2. 完全传播（attemptFull）：
   - 行中所有变量都是候选
   - 选择最佳变量传播

5.5 Farkas 证明
---------------
1. Farkas 引理：
   - 如果系统不可行，存在非负系数使得线性组合产生矛盾
   - 用于生成冲突证明

2. Farkas 系数：
   - 存储在 ConstraintRule 中
   - 用于构建证明树

================================================================================
六、文件结构
================================================================================

核心文件：
- theory_arith_private.h/cpp: 主实现类
- linear_solver.h/cpp: 线性求解器包装
- constraint.h/cpp: 约束和约束数据库
- partial_model.h/cpp: 部分模型和变量管理
- tableau.h/cpp: 单纯形表
- linear_equality.h/cpp: 线性等式模块
- dual_simplex.h/cpp: 对偶单纯形法
- fc_simplex.h/cpp: FC 单纯形法
- error_set.h/cpp: 错误集管理
- delta_rational.h: DeltaRational 实现
- arithvar.h: 算术变量类型定义
- normal_form.h/cpp: 范式处理

辅助文件：
- matrix.h/cpp: 矩阵实现
- simplex.h/cpp: 单纯形法基类
- simplex_update.h: 更新信息
- callbacks.h/cpp: 回调函数
- bound_counts.h: 边界计数
- congruence_manager.h/cpp: 一致性管理器

================================================================================
七、迁移注意事项
================================================================================

7.1 数据结构映射
----------------
- ArithVar: 映射到目标求解器的变量索引类型
- DeltaRational: 需要实现类似的无穷小量机制，或使用严格不等式处理
- Constraint: 需要实现约束类型和值的管理
- Tableau: 需要实现矩阵和基变量管理

7.2 算法实现
------------
1. 对偶单纯形法：
   - 需要实现转轴操作
   - 需要实现错误集管理
   - 需要实现变量选择策略

2. 约束处理：
   - 需要实现约束数据库
   - 需要实现约束传播
   - 需要实现冲突生成

3. 证明生成：
   - 需要实现 Farkas 证明
   - 需要维护证明树

7.3 关键接口
------------
1. 变量管理：
   - 创建变量
   - 设置赋值
   - 设置边界

2. 约束管理：
   - 添加约束
   - 查找约束
   - 传播约束

3. 求解接口：
   - check()
   - solveRealRelaxation()
   - findModel()

7.4 性能优化
------------
1. 稀疏矩阵表示
2. 增量更新
3. 缓存机制
4. 早期终止

================================================================================
八、参考文献
================================================================================

1. "A Fast Linear-Arithmetic Solver for DPLL(T)" by Leonardo de Moura and 
   Nikolaj Bjørner (CAV 2006)
   http://research.microsoft.com/en-us/um/people/leonardo/cav06.pdf

2. cvc5 源代码文档注释

3. 相关文件中的算法说明

================================================================================
结束
================================================================================

